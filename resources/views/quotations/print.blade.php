<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>{{ $quotation->quotation_number }}</title><style>body{font-family:Tahoma;margin:32px;color:#111}header{display:flex;justify-content:space-between;border-bottom:3px solid #b40000}table{width:100%;border-collapse:collapse;margin-top:24px}th,td{border:1px solid #bbb;padding:8px}.watermark{font-size:28px;color:#b40000}@media print{button{display:none}}</style></head><body>
<header><div><h1>{{ $quotation->branch->company->name }}</h1><p>{{ $quotation->branch->name }}</p></div><div><strong>{{ $quotation->quotation_number }} / V{{ $quotation->version_number }}</strong><p>{{ $quotation->quotation_date->format('Y-m-d') }}</p></div></header>
@if(in_array($quotation->status,['draft','expired','superseded','cancelled']))<p class="watermark">{{ strtoupper($quotation->status) }}</p>@endif
<p>العميل: {{ $quotation->customer->name }} — السيارة: {{ $quotation->vehicle->plate_number ?: $quotation->vehicle->vin }}</p>
<table><thead><tr><th>البيان</th><th>الكمية</th><th>السعر</th><th>الخصم</th><th>الضريبة</th><th>الإجمالي</th></tr></thead><tbody>@foreach($quotation->items as $item)<tr><td>{{ $item->description }}</td><td>{{ $item->quantity }}</td><td>{{ $item->unit_price }}</td><td>{{ $item->discount_amount }}</td><td>{{ $item->tax_amount }}</td><td>{{ $item->total }}</td></tr>@endforeach</tbody></table>
<h2>الإجمالي: {{ app(\App\Services\MoneyFormatter::class)->format($quotation->total, $quotation->currency) }}</h2><p>صالح حتى: {{ $quotation->valid_until->format('Y-m-d') }}</p><p>{!! nl2br(e($quotation->terms_and_conditions)) !!}</p><button onclick="window.print()">طباعة</button></body></html>
