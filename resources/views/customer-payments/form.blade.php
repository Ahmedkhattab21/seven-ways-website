@extends('layouts.app') @section('title','تسجيل دفعة') @section('page-title','تسجيل دفعة تشغيلية') @section('content')<form class="sw-card" method="POST" action="{{ route('customer-payments.store') }}">@csrf<select name="customer_id">@foreach($customers as $customer)<option value="{{ $customer->id }}">{{ $customer->name }}</option>@endforeach</select><select name="payment_method_id">@foreach($methods as $method)<option value="{{ $method->id }}">{{ $method->name }}</option>@endforeach</select><input type="date" name="payment_date" value="{{ today()->toDateString() }}"><input type="number" step="0.0001" name="amount" min="0.0001" required><input name="reference_number" placeholder="المرجع"><button class="sw-btn">تسجيل</button></form>@endsection
