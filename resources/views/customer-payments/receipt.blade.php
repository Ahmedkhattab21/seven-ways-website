<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>{{ $payment->payment_number }}</title><style>body{font-family:Arial;margin:40px}.brand{color:#b5121b;font-size:28px;font-weight:bold}</style></head><body><div class="brand">SEVEN WAYS</div><h1>إيصال دفعة تشغيلية</h1><p>{{ $payment->payment_number }} — {{ $payment->branch->name }}</p><p>العميل: {{ $payment->customer->name }}</p><p>المبلغ: {{ $payment->amount }} — الطريقة: {{ $payment->paymentMethod->name }} — المرجع: {{ $payment->reference_number }}</p><p>غير مخصص: {{ $payment->unallocated_amount }} — الحالة: {{ $payment->status }}</p>@foreach($payment->allocations->whereNull('reversed_at') as $allocation)<p>{{ $allocation->invoice->invoice_number }}: {{ $allocation->amount }}</p>@endforeach</body></html>
