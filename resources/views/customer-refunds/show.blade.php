@extends('layouts.app') @section('title',$refund->refund_number) @section('page-title',$refund->refund_number) @section('content')<div class="sw-card"><p>{{ $refund->customer->name }} — {{ $refund->amount }} — {{ $refund->status }}</p><p>تشغيلي فقط، لا ينشئ حركة بنك أو صندوق أو قيد.</p></div>@if($refund->status==='draft')<form method="POST" action="{{ route('customer-refunds.action',[$refund,'approve']) }}">@csrf<button class="sw-btn">اعتماد</button></form>@elseif($refund->status==='approved')<form method="POST" action="{{ route('customer-refunds.action',[$refund,'process']) }}">@csrf<button class="sw-btn">تنفيذ</button></form>@endif @endsection
