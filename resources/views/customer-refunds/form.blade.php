@extends('layouts.app') @section('title','Refund') @section('page-title','Refund تشغيلي') @section('content')<form class="sw-card" method="POST" action="{{ route('customer-refunds.store') }}">@csrf<select name="sales_credit_note_id">@foreach($notes as $note)<option value="{{ $note->id }}">{{ $note->credit_note_number }} — {{ $note->remaining_amount }}</option>@endforeach</select><select name="payment_method_id">@foreach($methods as $method)<option value="{{ $method->id }}">{{ $method->name }}</option>@endforeach</select><input type="date" name="refund_date" value="{{ today()->toDateString() }}"><input type="number" step="0.0001" name="amount"><input name="reason" placeholder="السبب"><button class="sw-btn">حفظ</button></form>@endsection
