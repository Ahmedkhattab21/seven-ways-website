@extends('layouts.app')
@section('title','الحجوزات')
@section('content')
<div class="sw-page-header"><div><h1>الحجوزات</h1><p>جدولة ما قبل التنفيذ دون أمر عمل أو حجز مخزون.</p></div><div><a class="sw-btn" href="{{ route('appointments.calendar') }}">التقويم</a>@if(auth()->user()->hasPermission('appointments.create'))<a class="sw-btn sw-btn--primary" href="{{ route('appointments.create') }}">إضافة حجز</a>@endif</div></div>
<form class="sw-filter-bar" method="GET"><select name="status"><option value="">كل الحالات</option>@foreach(['pending','confirmed','checked_in','in_progress','completed','cancelled','no_show'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ $status }}</option>@endforeach</select><select name="branch_id"><option value="">كل الفروع</option>@foreach($branches as $branch)<option value="{{ $branch->id }}" @selected(request('branch_id')==$branch->id)>{{ $branch->name }}</option>@endforeach</select><button class="sw-btn">تصفية</button></form>
<div class="sw-card sw-table-wrap"><table class="sw-table"><thead><tr><th>الرقم</th><th>العميل</th><th>البداية</th><th>الفني</th><th>الحالة</th><th>العربون</th></tr></thead><tbody>@forelse($appointments as $appointment)<tr><td><a href="{{ route('appointments.show',$appointment) }}">{{ $appointment->appointment_number }}</a></td><td>{{ $appointment->customer->name }}</td><td>{{ $appointment->scheduled_start->format('Y-m-d H:i') }}</td><td>{{ $appointment->assignedEmployee?->name??'غير مسند' }}</td><td>{{ $appointment->status }}</td><td>{{ $appointment->deposit_status }}</td></tr>@empty<tr><td colspan="6">لا توجد حجوزات.</td></tr>@endforelse</tbody></table></div>{{ $appointments->links() }}
@endsection
