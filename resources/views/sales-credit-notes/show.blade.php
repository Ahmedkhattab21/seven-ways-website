@extends('layouts.app') @section('title',$note->credit_note_number) @section('page-title',$note->credit_note_number) @section('content')<div class="sw-card"><p>{{ $note->invoice->invoice_number }} — {{ $note->customer->name }} — {{ $note->status }}</p><p>{{ $note->reason }} — {{ $note->total }}</p><a class="sw-btn" href="{{ route('sales-credit-notes.print',$note) }}">طباعة</a></div>@if($note->status==='draft')<form method="POST" action="{{ route('sales-credit-notes.action',[$note,'approve']) }}">@csrf<button class="sw-btn">اعتماد</button></form>@elseif($note->status==='approved')<form method="POST" action="{{ route('sales-credit-notes.action',[$note,'issue']) }}">@csrf<button class="sw-btn">إصدار</button></form>@endif @endsection
