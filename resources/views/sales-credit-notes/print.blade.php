<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>{{ $note->credit_note_number }}</title></head><body><img src="{{ asset(config('branding.logo_on_light')) }}" alt="Seven Ways" style="width:220px;height:auto"><h1>إشعار دائن</h1><p>{{ $note->credit_note_number }} — الفاتورة {{ $note->invoice->invoice_number }} — {{ $note->customer->name }} — {{ $note->status }}</p>@foreach($note->items as $item)<p>{{ $item->description }} — {{ $item->quantity }} — {{ $item->total }}</p>@endforeach<p>الإجمالي {{ $note->total }}</p></body></html>
