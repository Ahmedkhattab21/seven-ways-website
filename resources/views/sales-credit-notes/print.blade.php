<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>{{ $note->credit_note_number }}</title></head><body><h1>SEVEN WAYS — إشعار دائن</h1><p>{{ $note->credit_note_number }} — الفاتورة {{ $note->invoice->invoice_number }} — {{ $note->customer->name }} — {{ $note->status }}</p>@foreach($note->items as $item)<p>{{ $item->description }} — {{ $item->quantity }} — {{ $item->total }}</p>@endforeach<p>الإجمالي {{ $note->total }}</p></body></html>
