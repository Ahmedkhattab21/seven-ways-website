@extends('layouts.app') @section('title','إشعار دائن') @section('page-title','إشعار دائن') @section('content')<form class="sw-card" method="POST" action="{{ route('sales-credit-notes.store') }}">@csrf<input type="hidden" name="sales_invoice_id" value="{{ $invoice->id }}"><input type="date" name="credit_note_date" value="{{ today()->toDateString() }}"><select name="reason_code"><option value="service_refund">استرداد خدمة</option><option value="pricing_error">خطأ تسعير</option><option value="other">أخرى</option></select><input name="reason" required placeholder="السبب">@foreach($invoice->items as $i=>$item)<label><input type="checkbox" name="items[{{ $i }}][sales_invoice_item_id]" value="{{ $item->id }}">{{ $item->description }}</label><input type="number" step="0.000001" name="items[{{ $i }}][quantity]" value="{{ $item->quantity }}">@endforeach<button class="sw-btn">حفظ</button></form>@endsection
